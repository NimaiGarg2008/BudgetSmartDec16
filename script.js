/* Simple function to redirect the user to a different html webpage.
We use location.replace so the user can not use the back button on their browser
to go back and must navigate with buttons */
function goToNextPage(nextPage)
{
            location.replace(nextPage);
}
